{
  "version": "3.0",
  "name": "Java Generics → Cangjie Mapping Rule Library",
  "description": "系统化的、可执行的 Java 泛型机制到仓颉代码的提示规则库，覆盖声明、约束、通配符、型变、原始类型、数组实例化、递归边界、语义差异和需要代码重构的容器/API 模式共 9 大类 42 条规则；纯类型表达式映射由 java_base_type_map.json 与 type_expression.py 确定性处理。v3.0 新增：函数式接口映射、JDK 类型扩展、类型参数保留、嵌套类映射。",
  "source": "x2cangjie",
  "created": "2026-06-04",
  "updated": "2026-06-07",
  "rule_schema_version": "2.0",
  "rule_schema": {
    "id": "规则唯一标识，格式 C{NN}",
    "category": "规则大类：declaration | constraint | wildcard | variance | raw_type | array_instantiation | advanced | semantic_gap | container_smart",
    "subcategory": "子类，如 generic_class, upper_bound, unbounded_wildcard 等",
    "name": "规则中文名称",
    "java_ast_pattern": {
      "node_type": "tree-sitter AST 节点类型（可选）",
      "has_type_parameters": "是否包含类型参数（可选）",
      "constraint_type": "约束类型: unbounded | single_bound | multi_bound | recursive 等（可选）"
    },
    "java_regex_fallback": "正则表达式后备匹配（用于非 AST 场景）",
    "cangjie_template": "仓颉模板，使用 $1, $2 等占位符",
    "transforms": [
      {
        "from": "Java 语法特征",
        "to": "仓颉对应语法",
        "scope": "转换作用域",
        "comment": "说明（可选）"
      }
    ],
    "constraints": {
      "cangjie_semantics": "仓颉语义差异说明",
      "requires": ["前置条件规则 ID"],
      "conflicts": ["冲突规则 ID"],
      "action": "需执行的动作（可选）"
    },
    "priority": "匹配优先级，数字越大越优先",
    "example": {
      "java": "Java 示例代码",
      "cangjie": "仓颉示例代码",
      "note": "说明（可选）"
    },
    "validation_probes": ["编译验证检查项"]
  },
  "files": {
    "schema": "schema.json",
    "primitive_map": "primitive_map.json",
    "type_container_map": "type_container_map.json",
    "functional_interface_map": "functional_interface_map.json",
    "nested_class_map": "nested_class_map.json",
    "rules": [
      "rules/01_declaration.json",
      "rules/02_constraint.json",
      "rules/03_wildcard.json",
      "rules/04_variance.json",
      "rules/05_raw_type.json",
      "rules/06_array_instantiation.json",
      "rules/07_advanced.json",
      "rules/08_semantic_gap.json",
      "rules/09_container_smart.json"
    ]
  },
  "integration_priority": [
    "Java Base Type Map (java_base_type_map.json)",
    "Custom Types (custom_types.json)",
    "Deterministic Type Expression Resolver (java_base_type_map.json + type_expression.py)",
    "Generics Rule Lib (本规则库，仅泛型机制/语义差异提示)",
    "Progressive KB 缓存 (type_mappings.json)",
    "Progressive KB Few-Shot (translation_pool.json)",
    "RAG 文档检索",
    "LLM 推理"
  ]
}
